import 'package:flutter/material.dart';

double cardCornerRadius = 16;

double screenWidth(context) => MediaQuery.of(context).size.width;

double screenHeight(context) => MediaQuery.of(context).size.height;
